# covid_funding_denmark
Code for generating clean data and figures for studying research funding in Denmark during COVID-19.

To generate figures, run files in order starting with `2_final_data_cleaning.R`.

The file `1_new_data_cleaning.R` is for generating tabular, relational data, for import in SQL. This data is less clean, than the abovementioned.
